#page_header.wrapper
    .header
        header
            h1 #{tag + ' ' + post.title}
                -if (post.subtitle)
                    small #{post.subtitle}
            -if (post.published)
                .time
                    time(datetime="#{post.published}", pubdate) #{ciadc.pretty_date(post.published)}
