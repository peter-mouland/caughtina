#page_header
    header
        h1 #{post.title}
            -if (post.subtitle)
                small #{post.subtitle}
        -if (post.published)
            .time
                time(datetime="#{post.published}", pubdate) #{moment(post.published).format('Do MMMM YYYY')}
        -if (editable)
            a.button#save-page update